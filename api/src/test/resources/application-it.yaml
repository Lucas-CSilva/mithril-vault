logging:
  level:
    root: WARN
    com.mithrilvault.api: INFO

spring:
  cloud:
    aws:
      secretsmanager:
        enabled: false
  config:
    import: ""

app:
  jwt:
    secret-key: integration-test-secret-not-for-production-use!!
